animations {
    enabled = false
    bezier = linear, 0.0, 0.0, 1.0, 1.0

    animation = windows, 0, 1, linear, slide
    animation = windowsOut, 0, 1, linear, slide
    animation = windowsMove, 0, 1, linear, slide
    animation = border, 0, 1, linear
    animation = fade, 0, 1, linear
    animation = fadeIn, 0, 1, linear
    animation = fadeOut, 0, 1, linear
    animation = fadeSwitch, 0, 1, linear
    animation = fadeDim, 0, 1, linear
    animation = fadeShadow, 0, 1, linear
    animation = workspaces, 0, 1, linear, slide
    animation = specialWorkspace, 0, 1, linear, slidevert
    animation = layers, 0, 1, linear, fade
    animation = borderangle, 0, 8, linear, loop
}
